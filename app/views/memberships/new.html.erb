<div class="container">
  <%= simple_form_for [@group, @membership] do |f| %>
  <%= f.input :user, label: "Insira e-mail do usuário" %>
  <%= f.input :group_id, collection: [@group.group_name], include_blank: false, disabled: true %>
  <%= f.submit "Convidar", class:"btn btn-dark" %>
<% end %>
</div>
