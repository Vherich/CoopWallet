
<html>
<head>
    <title>CoopWallet - Homepage</title>
    <link rel="stylesheet" type="text/css" href="styles.css">
</head>
<body>
    <nav class="navbar">
        <a href="#">
            <img src="caminho/para/imagem-perfil.jpg" alt="Imagem de perfil">
        </a>
    </nav>

    <div class="banner"></div>

    <div class="app-info">
        <p>Dividas contas e despesas com as pessoas que mais importam para você!</p>
    </div>

    <div class="image"></div>

    <div class="input-container">
        <input type="text" placeholder="Nome" value="Nome">
        <input type="email" placeholder="E-mail" value="E-mail">
        <button class="signup-button">Cadastre</button>
    </div>
</body>

<canvas
  data-controller="chart"
  data-chart-data-value="<%= @chart_data.to_json %>"
  data-chart-options-value="<%= @chart_options.to_json %>"
></canvas>

<div class="container">
  <h1>Home</h1>

  <% if @groups.empty? %>
    <p>Você não está em nenhum grupo</p>
    <% else %>
    <% @groups.each do |group| %>
    <p><%= group.group_name %></p>
    <%= link_to 'Saídas', group_outcomes_path(group), class: "btn btn-dark"%>
    <%= link_to 'Entradas', group_incomes_path(group), class: "btn btn-dark"%>

    <% end %>

    <% end %>



</div>
</html>
